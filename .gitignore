# SSH Keys - NEVER commit private keys
*.pub
*.pem
*.key
*id_rsa*
*id_ed25519*
.ssh/

# Dependencies
node_modules/
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Raw/original photos — optimize-images.sh turns these into the
# .jpg/.webp pairs that actually ship, in src/images/. Keep the
# originals local only; don't bloat the repo with them.
rawPhotos/*
!rawPhotos/*.sh
